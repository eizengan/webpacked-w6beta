<!DOCTYPE html>
<html>
  <head>
    <title>Webpacker6</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#
      TRICKY: to enable CSS packs you must run
      yarn add css-loader mini-css-extract-plugin css-minimizer-webpack-plugin
    %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%#
      TRICKY: to enable React packs you must run
      yarn add react react-dom @babel/preset-react
      A separate, JSX entrypoint can be created with the same name as the
      JS pack; they will be consolidated into a single JS pack by Webpack
      during compilation
    %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <main>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
